export type Video = {
  id: string;
  title: string;
  duration: string;
  views: number;
  description: string;
  uploadedBy: string;
  uploadDate: string;
  video_file: string;
   thumbnail : string;
   uploaded_at: string;
   uploaded_by:string;// backend file path
};
